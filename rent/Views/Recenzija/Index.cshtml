@model IEnumerable<rent.Models.Recenzija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAutora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdResursa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ocjena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Komentar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAutora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdResursa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ocjena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Komentar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRecenzije">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRecenzije">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRecenzije">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
